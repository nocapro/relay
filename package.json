{
  "name": "relaycode-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "build": "bun --filter \"*\" build"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}